const int MAXQUESTS = 15;							// Maximum number of quests allowed.
const int LEG_QUEST_DEEDSHOUTS = 0;						// If Deed completions should be shouted.
const int LEG_QUEST_ICONS = 0;							// If quest icons should be enabled.
const int JOURNAL_OVERRIDE = 0;							// If we are using the journal override hak.
const string QUESTPREFIX = "leg_quest";			// Table prefix for Quest Plugin
