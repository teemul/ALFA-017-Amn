////////////////////////////////////////////////////////////////////////////////
//
//        $Date:: 2012-05-17#$ date the file was created or modified
//       Author : t-ice
//
//  Local Variable Prefix =
//
//  Dependencies external of nwscript:
//
//  Description
// Removes non-editable-by-tailor tags from all items a PC takes out of a
// tailor area
//
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Includes ////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

#include "acr_trigger_i"

////////////////////////////////////////////////////////////////////////////////
// Constants ///////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Structures //////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Global Variables ////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// Function Prototypes /////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

// The main event handler.
void main();

////////////////////////////////////////////////////////////////////////////////
// Function Definitions ////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////

void main() {
    ACR_TriggerOnExit();

    // Custom code goes here.
	object oPC = GetExitingObject();
	object oTestItem = GetFirstItemInInventory(oPC);
	int iTest = 0;
	SendMessageToPC(oPC, "Now leaving customizer area.");
	while (oTestItem != OBJECT_INVALID) {
		// check for items bought here to clear the merchant stamp
		iTest = GetLocalInt(oTestItem, "XC_NOT_CRAFTABLE");
		if (iTest == 17) {
			// Item has been set unmoddable by entering this area
			DeleteLocalInt(oTestItem, "XC_NOT_CRAFTABLE");
		}
		oTestItem = GetNextItemInInventory(oPC);
	}
	int nIndex = 0; // 0 = INVENTORY_SLOT_HEAD
	while (nIndex <= 18) {
		// loop through all 18 possible inventory slots to remove return variables.
		oTestItem = GetItemInSlot(nIndex, oPC);
		iTest = GetLocalInt(oTestItem, "XC_NOT_CRAFTABLE");
		if (iTest == 17) {
			// Item has been set unmoddable by entering this area
			DeleteLocalInt(oTestItem, "XC_NOT_CRAFTABLE");
		}
		nIndex = nIndex + 1;
	}
}